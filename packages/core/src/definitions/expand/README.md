### Expand

#### Options

- background (string)
- reverse (bool)
- cover (bool)

#### Usage

```
import { expand } from 'material-transitions-core';

expand(document.getElementById('start-element'), {
  autoStart: true,
  background: 'blue',
});
```
